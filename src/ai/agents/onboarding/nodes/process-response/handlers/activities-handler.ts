import { createSimpleHandler } from "./utils";

/**
 * Handle activities response: store as plain string
 * Uses the generic simple handler pattern
 */
export const handleActivitiesResponse = createSimpleHandler();
